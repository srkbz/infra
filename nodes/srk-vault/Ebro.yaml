environment:
  STATE_DIR: "${EBRO_ROOT}/.state"
  CACHE_DIR: "${EBRO_ROOT}/.cache"

imports:
  discover: { from: "../../modules/discover" }
  base-dirs: { from: "../../modules/base-dirs" }

tasks:
  default:
    requires: [base-dirs, discover]

  example:
    labels:
      autorequire: "true"
    script: echo Hello world
