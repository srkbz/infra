environment:
  STATE_DIR: "${EBRO_ROOT}/.state"
  CACHE_DIR: "${EBRO_ROOT}/.cache"

imports:
  discover: { from: "../../modules/discover" }
  zase-dirs: { from: "../../modules/base-dirs" }

tasks:
  dirs:
    required_by: [discover]
    requires: [zase-dirs]

  default:
    requires: [discover, dirs]

  example:
    labels:
      discover: "true"
    script: echo Hello world
