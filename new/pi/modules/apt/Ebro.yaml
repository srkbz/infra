tasks:
  default:
    script: cat "${EBRO_ROOT}/.cache/apt/packages.txt"

  clean:
    script: |
      set -x
      rm -rf  "${EBRO_ROOT}/.cache/apt"
      mkdir -p "${EBRO_ROOT}/.cache/apt"
      touch "${EBRO_ROOT}/.cache/apt/packages.txt"
