imports:
  apt:
    from: ../../modules/apt
  vpn:
    from: ../../modules/vpn
  docker:
    from: ../../modules/docker
  secrets:
    from: ../../modules/secrets

tasks:
  default:
    requires: [apt, vpn:server, docker, secrets]
