tasks:
  default:
    requires:
      - query: tasks | filter(.labels["discover"] == "true") | map(.id)

  before:
    required_by:
      - query: tasks | filter(.labels["discover"] == "true") | map(.id)
